# fastjson

Fast JSON parser for Python. Drop-in replacement for the stdlib `json` module with ~3x throughput on large documents.

## Install

```
pip install fastjson
```

## Usage

```python
import fastjson
fastjson.loads('{"hello": "world"}')
```
